package com.stochastictinkr.skywing.components.borders

import javax.swing.border.BevelBorder

enum class BevelType(val value: Int) {
    RAISED(BevelBorder.RAISED),
    LOWERED(BevelBorder.LOWERED)
}